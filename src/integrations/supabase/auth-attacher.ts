// Stub: this project is a Vite SPA (react-router-dom), not TanStack Start.
// The original auto-generated file imports `@tanstack/react-start`, which is
// not installed. Nothing in the app uses `attachSupabaseAuth`, so we export
// a harmless placeholder to keep the build green.
export const attachSupabaseAuth = null as unknown as never;
